# build output
black.exe
black.new.exe
black
black.new
*.o

# large third-party single header (downloaded via VOICE-SETUP.md)
miniaudio.h

# runtime/local state (regenerated at runtime)
desk_user.txt
desk_online.txt
desk_chat.txt
desk_auth_body.tmp

# SECRETS / account data — never commit these
auth-secret.key
auth-users.json
users.json

# server-side player data (regenerated; not meant for the repo)
friends.json
leaderboard.json
